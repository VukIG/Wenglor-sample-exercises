#ifndef FUNKCIJE_H
#define FUNKCIJE_H

#include <stdio.h>  
#include <stdlib.h>
#include <math.h>

float calculateResult(float *numbers, int count, char operation);
void convertToInteger(char *line);

#endif